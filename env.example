FROM_EMAIL=
FROM_EMAIL_PASS=
ADMIN_EMAIL=
NEXT_PUBLIC_SITE_URL=

# Google Analytics 4 — Measurement ID from Admin → Data streams (format G-XXXXXXXXXX)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
# Set to true only when you want hits from `next dev` (default: off to keep metrics clean)
NEXT_PUBLIC_GA_ENABLE_IN_DEV=
